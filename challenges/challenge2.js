/**
 * Challenge 2.
 *
 * Return an array with all the keys of the Object «person».
 *
 * @param {object} person
 *
 * Try to use a more functional approach using map/reduce/forEach, instead
 * of a procedural loop (while, for).
 */
const getTheObjectKeys = person => {
  console.log("Returning the keys of", person);

  let objectKeys;

  // YOUR CODE

  return objectKeys;
}

module.exports = {
  getTheObjectKeys,
}
