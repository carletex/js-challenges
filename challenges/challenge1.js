
const numbers = [7, 5, 1, 15, 6, 85, 1, 22];

/**
 * Challenge 1.
 *
 * Return the average of the numbers in the variable «numbers».
 *
 * Try to use a more functional approach using map/reduce/forEach, instead
 * of a procedural loop (while, for).
 */
const average = () => {
  console.log("Computing the average of", numbers);

  let numberAverage;

  // YOUR CODE

  return numberAverage;
}

module.exports = {
  average,
}
